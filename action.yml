name: 'Benchmark CI'
description: ''

inputs:
  directory:
    description: 'Path to the folder of benchmarks'
    required: true
  threshold:
    description: 'A percentage that defines the threshold for performance regressions.'
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'